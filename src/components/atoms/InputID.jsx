import React from 'react';
import TextField from '@material-ui/core/TextField';

function InputID() {
    return (
        <TextField
            id="inputID"
            label="ID"
            variant="outlined"
        />
    );
}

export default InputID;